name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Explicit read permission for checkout — required when the org default workflow
# token is restricted. Harmless if permissions are already open.
permissions:
  contents: read

jobs:
  backend:
    name: Backend — lint + tests + import smoke
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r ../requirements.txt

      - name: Import smoke test (catches dead imports before runtime)
        # Import the deployed entrypoint (main:app, per app.yaml/start.py), not
        # just bootstrap — main.py hosts route wiring CI would otherwise miss. (Audit H3)
        run: python -c "import main; print('main import OK')"

      - name: Run tests
        run: python -m pytest tests -q --tb=short

  frontend:
    name: Frontend — lint + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
